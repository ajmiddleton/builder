.tree-container
  .tree(data-id=tree._id.toString(), class=treeHelper.getClass(tree.height), class=tree.isHealthy ? 'alive' : 'dead')
    .height= tree.height
    .health= tree.isHealthy
  if treeHelper.getClass(tree.height) === 'adult'
    button.button.button-caution.chop Chop!
